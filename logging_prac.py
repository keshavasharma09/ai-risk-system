# import logging

# # Configure the logger to show ALL levels (DEBUG and above)

# logging.basicConfig(level = logging.DEBU, format="%(levelname)s: %(message)s")
# # Write your log messages

# logging.debug("This is a detailed diagnostic message")
# logging.info("The application has started successfully")
# logging.warning("User attempted a weak password.")
# logging.error("Failed to connect to the database.")
# logging.critical("System out of memory! Shutting down.")


# # Configure the logger with a custom format

# logging.basicConfig(level = logging.INFO, format = "%(asctime)s - %(levelname)s - %(message)s")